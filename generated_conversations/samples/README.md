# Sample Conversation Run

This folder contains one small representative conversation run with one example per label (`covered`, `not_covered`, `edge_case`).

Full generated conversation runs are excluded from version control by default.
